describe('apiService', () => {
    describe('parameter handling', () => {
        it('should throw the expected error if no url param is passed', () => {

        });
    });

    describe('error handling', () => {
        it('should handle fetch errors as expected', () => {

        });
    });

    describe('fetch options', () => {
        it('should behave as expected with no options param passed', () => {

        });

        it('should utilise options param correctly', () => {

        });
    });

    describe('response stream parsing', () => {
        it('should parse JSON repsonses correctly', () => {

        });

        it('should parse text response correctly', () => {

        });
    });
});
