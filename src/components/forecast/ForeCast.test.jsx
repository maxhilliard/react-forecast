describe('Forecast', () => {
    describe('snapshot', () => {
        it('should match snapshot', () => {

        });
    });

    describe('props', () => {
        it('should display default view if no forecast prop is passed', () => {

        });

        it('should dislay a ForecastTile component for each day of the forecast array', () => {

        });
    });
});
