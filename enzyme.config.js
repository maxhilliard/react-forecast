// import * as config from './src/config/config.json';

const testConfig = {
    weatherApi: {
        apiBaseUrl: 'http://api.openweathermap.org/data/2.5/forecast/daily',
        apiKey: 'REPLACE_ME',
    },
};

window.config = testConfig;
